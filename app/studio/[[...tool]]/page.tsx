export const dynamic = "force-dynamic";

// Studio is disabled until Sanity credentials are configured.
// To enable: replace this file with the NextStudio import and config.
export default function StudioPage() {
  return (
    <div style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>CMS Studio</h1>
      <p>Studio will be available once Sanity credentials are configured.</p>
    </div>
  );
}
